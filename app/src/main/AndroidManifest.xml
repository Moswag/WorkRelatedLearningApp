<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jonathanmoreno.workrelatedlearning">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/wrl"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppThemefull">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ProfileActivity"
            android:theme="@style/AppThemefull" />
        <activity
            android:name=".SignupActivity"
            android:theme="@style/AppThemefull" />
        <activity
            android:name=".ResetPasswordActivity"
            android:theme="@style/AppThemefull" />
        <activity
            android:name=".NavMain"
            android:label="@string/title_activity_nav_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".VacancyDetailed" />
        <activity android:name=".activities.ViewVacancyByLocation" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.ViewVacancyByCompany"></activity>
    </application>

</manifest>